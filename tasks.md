# TeamFlow MVP Tasks

`plan.md`를 구현 단위로 분해한 체크리스트다. 각 항목은 완료 시 체크하고, 가능한 한 각 Phase가 독립 실행 가능한 상태를 유지한다.

## 공통 규칙
- [ ] 백엔드 응답은 엔티티가 아닌 DTO만 사용한다.
- [ ] 요청 DTO에는 `jakarta.validation`을 적용한다.
- [ ] 전역 예외 처리와 공통 에러 응답 형식을 유지한다.
- [ ] H2 in-memory 기준으로 로컬 실행이 가능해야 한다.
- [ ] 샘플 데이터가 있거나 수동 테스트 가능한 기본 데이터 준비 상태를 유지한다.
- [ ] README에 현재 실행 방법과 확인 절차를 반영한다.

## Phase 0. 프로젝트 베이스라인

### Backend
- [x] 패키지 구조를 `common`, `auth`, `user`, `task`, `share`, `comment`, `notification` 기준으로 정리한다.
- [x] 공통 예외 클래스와 `@RestControllerAdvice` 기반 글로벌 예외 핸들러를 만든다.
- [x] 공통 응답 구조 또는 API 에러 응답 포맷을 정의한다.
- [x] H2 연결과 JPA 기본 설정을 `application.properties`에 반영한다.
- [x] CORS 개발 환경 허용 설정을 추가한다.
- [x] 헬스체크용 기본 API를 추가한다.

### Frontend
- [x] `frontend/` 기준 Next.js App Router 프로젝트를 생성한다.
- [x] TypeScript와 Tailwind CSS 초기 설정을 완료한다.
- [x] 기본 라우트 구조(`/`, `/login`, `/my-tasks`, `/shared`)를 만든다.
- [x] 공통 레이아웃과 기본 네비게이션 뼈대를 만든다.
- [x] 백엔드 API 베이스 URL 환경 변수 구조를 정리한다.
- [x] 기본 홈 페이지가 정상 렌더링되도록 구성한다.

### 검증
- [x] 백엔드가 단독 실행된다.
- [x] 프론트엔드가 단독 실행된다.
- [x] 헬스체크 API 호출이 성공한다.
- [x] 기본 페이지 렌더링이 성공한다.

## Phase 1. 인증 최소 기능

### Backend
- [x] `User` 엔티티를 정의한다.
- [x] `UserRepository`를 추가한다.
- [x] 로그인 요청/응답 DTO를 만든다.
- [x] `AuthService`에 계정 조회와 비밀번호 비교 로직을 구현한다.
- [x] `POST /api/auth/login` 컨트롤러를 구현한다.
- [x] 로그인 실패 케이스에 대한 예외와 에러 메시지를 정의한다.
- [x] 샘플 사용자 계정을 준비한다.

### Frontend
- [x] `/login` 페이지 UI를 구현한다.
- [x] 이메일/비밀번호 입력 폼과 유효성 메시지를 구현한다.
- [x] 로그인 API 연동 함수를 추가한다.
- [x] 로그인 성공 시 세션 상태를 저장하는 간단한 클라이언트 로직을 구현한다.
- [x] 로그인 실패 시 사용자에게 오류 메시지를 노출한다.
- [x] 비로그인 상태에서 보호 페이지 접근 처리 방식을 정한다.

### 검증
- [x] 샘플 계정으로 로그인된다.
- [ ] 잘못된 계정/비밀번호 입력 시 실패 응답과 UI 메시지가 동작한다.

## Phase 2. 빠른 할 일 등록 + 내 할 일 기본 조회

### Backend
- [x] `TaskStatus` Enum을 정의한다.
- [x] `Task` 엔티티를 정의한다.
- [x] `TaskRepository`를 추가한다.
- [x] 태스크 생성 요청/응답 DTO를 만든다.
- [x] 내 태스크 조회 응답 DTO를 만든다.
- [x] `POST /api/tasks`를 구현한다.
- [x] `GET /api/tasks/my?filter=today|upcoming|all|done`를 구현한다.
- [x] `createdAt`, `updatedAt`, `dueAt`, `completedAt` 처리 기준을 반영한다.
- [x] today/upcoming/all/done 필터 쿼리를 분리 또는 명확하게 구현한다.

### Frontend
- [x] 홈 상단 `QuickAddBar` 컴포넌트를 구현한다.
- [x] 엔터 입력만으로 태스크 생성이 가능하도록 연결한다.
- [x] 내 할 일 리스트 기본 UI를 구현한다.
- [x] 필터 탭(today/upcoming/all/done)을 구현한다.
- [x] 태스크 행에 상태, 제목, 마감일을 표시한다.
- [x] 태스크 생성 후 리스트 재조회 또는 낙관적 갱신 흐름을 구현한다.
- [x] 로딩/에러/빈 상태 UI를 추가한다.

### 검증
- [x] 제목만으로 태스크를 생성할 수 있다.
- [x] 각 필터가 의도대로 목록을 구분한다.
- [ ] 생성 직후 리스트에 태스크가 반영된다.

## Phase 3. 태스크 상세/수정/삭제 + 상태 변경

### Backend
- [ ] 태스크 상세 조회 DTO를 정의한다.
- [ ] 태스크 수정 요청 DTO를 정의한다.
- [ ] 상태 변경 요청 DTO를 정의한다.
- [ ] `GET /api/tasks/{id}`를 구현한다.
- [ ] `PUT /api/tasks/{id}`를 구현한다.
- [ ] `PATCH /api/tasks/{id}/status`를 구현한다.
- [ ] `DELETE /api/tasks/{id}`를 구현한다.
- [ ] DONE 전환 시 `completedAt`이 설정되도록 처리한다.
- [ ] 권한 체크 로직을 작성자/담당자/공유 대상 기준으로 구현한다.
- [ ] 권한 없는 접근에 대해 403을 반환한다.

### Frontend
- [ ] `TaskDetailDrawer`를 구현한다.
- [ ] 태스크 클릭 시 상세 조회 API를 호출한다.
- [ ] 제목, 설명, 마감일, 담당자 등 수정 UI를 구현한다.
- [ ] 상태 변경 UI를 구현한다.
- [ ] 삭제 액션과 확인 흐름을 구현한다.
- [ ] 수정/삭제/상태 변경 후 리스트와 상세 화면 상태를 동기화한다.

### 검증
- [ ] 상세 패널에서 조회/수정/상태 변경이 가능하다.
- [ ] 작성자만 삭제할 수 있다.
- [ ] 권한 없는 사용자는 403을 받는다.

## Phase 4. 공유 기능

### Backend
- [ ] `TaskShare` 엔티티를 정의한다.
- [ ] `(task, user)` 유니크 제약을 반영한다.
- [ ] `TaskShareRepository`를 추가한다.
- [ ] 공유 추가 요청 DTO를 만든다.
- [ ] `POST /api/tasks/{id}/shares`를 구현한다.
- [ ] `DELETE /api/tasks/{id}/shares/{userId}`를 구현한다.
- [ ] `GET /api/tasks/shared`를 구현한다.
- [ ] 담당자 1명, 공유 대상 N명 정책을 서비스 로직에 반영한다.

### Frontend
- [ ] `/shared` 페이지를 구현한다.
- [ ] 나에게 할당된 일과 나에게 공유된 일을 구분해서 보여줄지 UX를 확정한다.
- [ ] 상세 패널에 담당자/공유 대상 편집 UI를 추가한다.
- [ ] 공유 추가/삭제 액션을 API와 연동한다.

### 검증
- [ ] 특정 사용자에게 공유를 추가할 수 있다.
- [ ] 공유를 삭제할 수 있다.
- [ ] 나에게 할당되었거나 공유된 태스크를 조회할 수 있다.

## Phase 5. 댓글 기능

### Backend
- [ ] `TaskComment` 엔티티를 정의한다.
- [ ] `TaskCommentRepository`를 추가한다.
- [ ] 댓글 생성 요청/응답 DTO를 만든다.
- [ ] `POST /api/tasks/{id}/comments`를 구현한다.
- [ ] `GET /api/tasks/{id}/comments`를 구현한다.
- [ ] 댓글 작성 권한을 작성자/담당자/공유 대상 기준으로 검사한다.
- [ ] 댓글 내용 필수 Validation을 추가한다.

### Frontend
- [ ] 상세 패널에 댓글 리스트 UI를 구현한다.
- [ ] 댓글 입력 폼을 구현한다.
- [ ] 댓글 작성 후 리스트를 갱신한다.
- [ ] 태스크 행에 댓글 수를 표시한다.
- [ ] 댓글 로딩/빈 상태를 반영한다.

### 검증
- [ ] 댓글 등록과 조회가 가능하다.
- [ ] 권한 없는 사용자의 댓글 작성이 차단된다.

## Phase 6. 기본 알림

### Backend
- [ ] `NotificationType` Enum을 정의한다.
- [ ] `Notification` 엔티티를 정의한다.
- [ ] `NotificationRepository`를 추가한다.
- [ ] 알림 조회 응답 DTO를 만든다.
- [ ] `GET /api/notifications`를 구현한다.
- [ ] `PATCH /api/notifications/{id}/read`를 구현한다.
- [ ] 담당자 지정/변경 시 ASSIGNED 알림 생성 로직을 구현한다.
- [ ] 공유 추가 시 SHARED 알림 생성 로직을 구현한다.
- [ ] 댓글 작성 시 COMMENTED 알림 생성 로직을 구현한다.
- [ ] 댓글 작성자 제외 관련 사용자에게만 알림이 가도록 처리한다.

### Frontend
- [ ] 알림 패널 UI를 구현한다.
- [ ] 알림 목록 조회와 읽음 처리 연동을 구현한다.
- [ ] 실시간 대신 재조회 또는 폴링 전략을 적용한다.
- [ ] 읽음/미읽음 상태를 시각적으로 구분한다.

### 검증
- [ ] 이벤트 발생 시 알림이 생성된다.
- [ ] 사용자별 알림 조회가 가능하다.
- [ ] 읽음 처리 후 상태가 반영된다.

## Phase 7. 샘플 데이터/통합 검증/문서화

### 데이터
- [ ] 샘플 데이터 주입 방식을 `data.sql` 또는 `CommandLineRunner` 중 하나로 확정한다.
- [ ] 사용자 3명 이상 샘플 데이터를 준비한다.
- [ ] 개인 태스크, 담당자 지정 태스크, 공유 태스크, 완료 태스크 샘플을 준비한다.
- [ ] 댓글 샘플을 준비한다.
- [ ] 알림 샘플을 준비한다.

### 검증
- [ ] 로그인 → 빠른 등록 → 공유 → 댓글 → 알림 확인 시나리오를 수동 검증한다.
- [ ] 권한 없는 접근 차단 시나리오를 검증한다.
- [ ] 클린 실행 후 즉시 데모 가능한지 확인한다.

### 문서화
- [ ] README에 프로젝트 개요, 기술 스택, 실행 방법을 정리한다.
- [ ] 백엔드/프론트엔드 실행 명령을 명시한다.
- [ ] 샘플 계정과 검증 시나리오를 문서화한다.
- [ ] 현재 구현 범위와 비범위를 명시한다.

## 우선순위 제안
- [ ] 1순위: Phase 0 완료
- [ ] 2순위: Phase 1 완료
- [ ] 3순위: Phase 2 완료
- [ ] 4순위: Phase 3 완료
- [ ] 5순위: Phase 4 완료
- [ ] 6순위: Phase 5 완료
- [ ] 7순위: Phase 6 완료
- [ ] 8순위: Phase 7 완료
